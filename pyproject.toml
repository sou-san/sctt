[project]
name = "sctt"
version = "0.1.0"
description = "SpeedCubeTimer-TUI"
readme = "README.md"
authors = [
    { name = "sou-san", email = "sou887867@gmail.com" } 
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
sctt = "sctt:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "mypy>=1.12.0",
    "ruff>=0.6.9",
    "textual-dev>=1.6.1",
]
